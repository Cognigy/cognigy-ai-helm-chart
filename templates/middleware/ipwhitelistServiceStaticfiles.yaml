{{- if and (.Values.ingress.enabled) (.Values.ingress.serviceStaticFiles.ipWhiteListMiddleware.enabled) (.Values.cognigyApps.enabled) }}
{{- with .Values.ingress.serviceStaticFiles.ipWhiteListMiddleware }}
---
# Whitelisting Based on `X-Forwarded-For` with `depth=0`
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: ipwhitelistservicestaticfiles
  namespace: {{ $.Release.Namespace | quote }}
spec:
  ipWhiteList: {{- toYaml .ipWhiteList | nindent 4 }}
{{- end }}
{{- end }}