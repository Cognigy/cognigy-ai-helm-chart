{{- if and (.Values.ingress.enabled) (.Values.ingress.serviceUi.ipWhiteListMiddleware.enabled) }}
{{- with .Values.ingress.serviceUi.ipWhiteListMiddleware }}
---
# Whitelisting Based on `X-Forwarded-For` with `depth=0`
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: ipwhitelistserviceui
  namespace: {{ $.Release.Namespace | quote }}
spec:
  ipWhiteList: {{- toYaml .ipWhiteList | nindent 4 }}
{{- end }}
{{- end }}