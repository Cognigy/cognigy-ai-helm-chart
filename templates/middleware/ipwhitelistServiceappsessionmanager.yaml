{{- if and (.Values.traefik.enabled) (.Values.ingress.enabled) (.Values.ingress.serviceAppSessionManager.ipWhiteListMiddleware.enabled) (.Values.cognigyApps.enabled) }}
{{- with .Values.ingress.serviceAppSessionManager.ipWhiteListMiddleware }}
---
# Whitelisting Based on `X-Forwarded-For` with `depth=0`
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: ipwhitelistserviceappsessionmanager
  namespace: {{ $.Release.Namespace | quote }}
spec:
  ipWhiteList: {{- toYaml .ipWhiteList | nindent 4 }}
{{- end }}
{{- end }}