{{- $cognigyInsightsJwt_secret := include "common.secretName.render" ( dict "existingSecret" $.Values.commonSecrets.cognigyInsightsJwt.existingSecret "defaultSecret" "cognigy-insights-jwt") }}
{{- $statefulRabbitMq_secret := include "common.secretName.render" ( dict "existingSecret" $.Values.statefulRabbitMq.auth.existingSecret "defaultSecret" "cognigy-rabbitmq") }}
{{- $redisHa_secret := include "common.secretName.render" ( dict "existingSecret" $.Values.redisHa.auth.existingSecret "defaultSecret" "cognigy-redis-password") }}
{{- $serviceInsightsApi_dbSecret :=  ternary (include "common.secretName.render" ( dict "existingSecret" (tpl .Values.insights.postgresql.auth.insightsUser.existingSecret .) "defaultSecret" "cognigy-insights-postgres-ha-insights")) (include "cubejs.postgresql.password.existingSecret" $) $.Values.insights.postgresql.usePartitionedTables }}
{{- $serviceInsightsApi_mongoDbSecret := include "common.secretName.render" ( dict "existingSecret" $.Values.dbConnectionString.services.serviceInsightsApi.auth.existingSecret "defaultSecret" (printf "cognigy-%s" $.Values.dbConnectionString.services.serviceInsightsApi.serviceName)) }}
{{- with .Values.serviceInsightsApi }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-insights-api
  namespace: {{ $.Release.Namespace | quote }}
  labels:
    owner-team: gold
spec:
  selector:
    matchLabels:
      app: service-insights-api
  replicas: {{ .replicaCount }}
  template:
    metadata:
      labels:
        app: service-insights-api
      annotations:
        checksum/cognigy-env: {{ include (print $.Template.BasePath "/configurations/cognigy-env.yaml") $ | sha256sum }}
        checksum/rabbitmq: {{ if not (lookup "v1" "Secret" $.Release.Namespace $statefulRabbitMq_secret) }}{{ $statefulRabbitMq_secret | sha256sum }}{{ else }}{{ toYaml (lookup "v1" "Secret" $.Release.Namespace $statefulRabbitMq_secret).data | sha256sum }}{{ end }}
        checksum/redis: {{ if not (lookup "v1" "Secret" $.Release.Namespace $redisHa_secret) }}{{ $redisHa_secret | sha256sum }}{{ else }}{{ toYaml (lookup "v1" "Secret" $.Release.Namespace $redisHa_secret).data | sha256sum }}{{ end }}
        checksum/mongodb: {{ if not (lookup "v1" "Secret" $.Release.Namespace $serviceInsightsApi_mongoDbSecret) }}{{ $serviceInsightsApi_mongoDbSecret | sha256sum }}{{ else }}{{ toYaml (lookup "v1" "Secret" $.Release.Namespace $serviceInsightsApi_mongoDbSecret).data | sha256sum }}{{ end }}
        {{- if and ($.Values.pgoperator.enabled) }}
        checksum/postgres: {{ if not (lookup "v1" "Secret" $.Release.Namespace $serviceInsightsApi_dbSecret) }}{{ $serviceInsightsApi_dbSecret | sha256sum }}{{ else }}{{ toYaml (lookup "v1" "Secret" $.Release.Namespace $serviceInsightsApi_dbSecret).data | sha256sum }}{{ end }}
        {{- end }}
        {{- if $.Values.cognigyCommonPodAnnotations }}
        {{- include "common.tplvalues.render" (dict "value" $.Values.cognigyCommonPodAnnotations "context" $) | nindent 8 }}
        {{- end }}
    spec:
      {{- if and $.Values.serviceInsightsApi.conversationAnalyzer.enabled $.Values.serviceInsightsApi.serviceAccountName }}
      serviceAccountName: {{ $.Values.serviceInsightsApi.serviceAccountName }}
      {{- end }}
      {{- if .affinity }}
      affinity: {{- include "common.tplvalues.render" (dict "value" .affinity "context" $) | nindent 8 }}
      {{- end }}
      {{- if .nodeSelector }}
      nodeSelector: {{- include "common.tplvalues.render" (dict "value" .nodeSelector "context" $) | nindent 8 }}
      {{- end }}
      {{- if .tolerations }}
      tolerations: {{- include "common.tplvalues.render" (dict "value" .tolerations "context" $) | nindent 8 }}
      {{- end }}
      {{- if .priorityClassName }}
      priorityClassName: {{ .priorityClassName }}
      {{- end }}
      containers:
        - name: service-insights-api
          image: {{ include "common.image.render" (dict "global" $.Values.global "image" .image) }}
          resources: {{- toYaml .resources | nindent 12 }}
          ports:
            - name: metrics
              containerPort: 8002
          envFrom:
            - configMapRef:
                name: cognigy-env
          env:
            {{- if and ($.Values.pgoperator.enabled) ($.Values.insights.postgresql.usePartitionedTables) ($.Values.pgoperator.postgresqlClusterInsights.enabled) }}
            - name: INSIGHTS_POSTGRES_DB_NAME
              value: {{ $.Values.insights.postgresql.database.name | quote }}
            - name: INSIGHTS_POSTGRES_DB_HOST
              value: {{ include "common.tplvalues.render" (dict "value" $.Values.insights.postgresql.cluster.host "context" $) | quote }}
            {{- if or $.Values.insights.postgresql.cluster.readReplica.useReadReplica .useReadReplica }}
            - name: INSIGHTS_POSTGRES_DB_REPLICA_HOST
              value: {{ include "common.tplvalues.render" (dict "value" $.Values.insights.postgresql.cluster.readReplica.host "context" $) | quote }}
            {{- end }}
            - name: INSIGHTS_POSTGRES_DB_PORT
              value: {{ $.Values.insights.postgresql.cluster.port | quote }}
            - name: INSIGHTS_POSTGRES_DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: {{ $serviceInsightsApi_dbSecret }}
                  key: username
            - name: INSIGHTS_POSTGRES_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ $serviceInsightsApi_dbSecret }}
                  key: password
            {{- end }}
            {{- if and ($.Values.pgoperator.enabled) ($.Values.cubejs.enabled) (not $.Values.insights.postgresql.usePartitionedTables) ($.Values.pgoperator.postgresqlCluster.enabled) }}
            - name: INSIGHTS_POSTGRES_DB_NAME
              value: {{ $.Values.cubejs.postgresql.database | quote }}
            - name: INSIGHTS_POSTGRES_DB_HOST
              value: {{ include "common.tplvalues.render" (dict "value" $.Values.cubejs.postgresql.host "context" $) | quote }}
            {{- if or $.Values.cubejs.postgresql.readReplica.useReadReplica .useReadReplica }}
            - name: INSIGHTS_POSTGRES_DB_REPLICA_HOST
              value: {{ include "common.tplvalues.render" (dict "value" $.Values.cubejs.postgresql.readReplica.host "context" $) | quote }}
            {{- end }}
            - name: INSIGHTS_POSTGRES_DB_PORT
              value: {{ $.Values.cubejs.postgresql.port | quote }}
            - name: INSIGHTS_POSTGRES_DB_USERNAME
              value: {{ $.Values.cubejs.postgresql.username | quote }}
            - name: INSIGHTS_POSTGRES_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ $serviceInsightsApi_dbSecret }}
                  key: password
            {{- end }}
            - name: REDIS_ENABLE_RECONNECT
              value: "true"
            - name: RABBITMQ_ENABLE_RECONNECT
              value: "true"
            - name: INSIGHTS_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ $cognigyInsightsJwt_secret }}
                  key: secret
            - name: TRACING_ENABLED
              value: {{ $.Values.tracing.sentry.insights.enabled | quote }}
            - name: TRACING_BASE_URL_WITH_PROTOCOL
              value: {{ $.Values.tracing.sentry.insights.baseUrl | quote }}      
            - name: TRACING_ERRORS_ENABLED
              value: {{ $.Values.tracing.sentry.insights.errorsEnabled | quote }}
            - name: TRACING_EVENTS_SAMPLE_RATE
              value: {{ $.Values.tracing.sentry.insights.eventsSampleRate | quote }}
            - name: TRACING_SAMPLE_RATE
              value: {{ $.Values.tracing.sentry.insights.sampleRate | quote }}
            - name: ENVIRONMENT
              value: {{ $.Values.tracing.environment | quote }}
            - name: COGNIGY_AI_RELEASE_VERSION
              value: {{ $.Chart.AppVersion | quote }}
            {{- if $.Values.logRedaction.enabled }}
            - name: LOG_REDACTION_ENABLED
              value: "true"
            {{- end }}
            {{- if $.Values.serviceInsightsApi.conversationAnalyzer.enabled }}
            - name: ANALYTICS_COLLECTOR_MONGODB_URI
              valueFrom:
                secretKeyRef:
                  name: {{ include "common.secretName.render" ( dict "existingSecret" $.Values.dbConnectionString.services.serviceAnalyticsCollector.auth.existingSecret "defaultSecret" (printf "cognigy-%s" $.Values.dbConnectionString.services.serviceAnalyticsCollector.serviceName)) }}
                  key: connection-string
            - name: KUBERNETES_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: SERVICE_INSIGHTS_API_IMAGE
              value: {{ include "common.image.render" (dict "global" $.Values.global "image" .image) }}
            - name: ANALYZER_LLM_REFERENCE_ID
              value: {{ $.Values.serviceInsightsApi.conversationAnalyzer.llmReferenceId | default "" | quote }}
            - name: ANALYZER_RESULTS_TTL_DAYS
              value: {{ $.Values.serviceInsightsApi.conversationAnalyzer.resultsTtlDays | default 30 | quote }}
            - name: ANALYZER_MAX_CONCURRENT_JOBS
              value: {{ $.Values.serviceInsightsApi.conversationAnalyzer.maxConcurrentJobs | default 100 | quote }}
            - name: ANALYZER_DISPATCH_INTERVAL_MS
              value: {{ $.Values.serviceInsightsApi.conversationAnalyzer.dispatchIntervalMs | default 30000 | quote }}
            - name: ANALYZER_JOB_BACKOFF_LIMIT
              value: {{ $.Values.serviceInsightsApi.conversationAnalyzer.bulkAnalysisJob.backoffLimit | default 0 | quote }}
            - name: ANALYZER_JOB_TTL_SECONDS
              value: {{ $.Values.serviceInsightsApi.conversationAnalyzer.bulkAnalysisJob.ttlSecondsAfterFinished | default 3600 | quote }}
            - name: ANALYZER_JOB_ACTIVE_DEADLINE
              value: {{ $.Values.serviceInsightsApi.conversationAnalyzer.bulkAnalysisJob.activeDeadlineSeconds | default 7200 | quote }}
            - name: ANALYZER_JOB_POSTGRESQL_SECRET
              value: {{ $serviceInsightsApi_dbSecret | quote }}
            {{- if $.Values.serviceInsightsApi.serviceAccountName }}
            - name: ANALYZER_JOB_SERVICE_ACCOUNT
              value: {{ $.Values.serviceInsightsApi.serviceAccountName | quote }}
            {{- end }}
            {{- end }}
            {{- if .extraEnvVars }}
            {{- include "common.tplvalues.render" (dict "value" .extraEnvVars "context" $) | nindent 12 }}
            {{- end }}
          volumeMounts:
            - name: rabbitmq-connection-string
              mountPath: /var/run/secrets/rabbitmqConnectionString
              subPath: rabbitmqConnectionString
            - name: mongodb-connection-string
              mountPath: /var/run/secrets/mongodbConnectionString
              subPath: mongodbConnectionString
            - name: redis-password
              mountPath: /var/run/secrets/redis-password.conf
              subPath: redis-password.conf
            {{- if .extraVolumeMounts }}
            {{- include "common.tplvalues.render" ( dict "value" .extraVolumeMounts "context" $ ) | nindent 12 }}
            {{- end }}
          startupProbe:
            httpGet:
              path: /health/startup
              port: 8001
          livenessProbe:
            httpGet:
              path: /health/liveness
              port: 8001
            failureThreshold: 3
            initialDelaySeconds: 90
            periodSeconds: 60
            timeoutSeconds: 10
      volumes:
        - name: rabbitmq-connection-string
          secret:
            secretName: {{ $statefulRabbitMq_secret }}
            items:
              - key: connection-string
                path: rabbitmqConnectionString
        - name: mongodb-connection-string
          secret:
            secretName: {{ $serviceInsightsApi_mongoDbSecret }}
            items:
              - key: connection-string
                path: mongodbConnectionString
        - name: redis-password
          secret:
            secretName: {{ $redisHa_secret }}
        {{- if .extraVolumes }}
        {{- include "common.tplvalues.render" ( dict "value" .extraVolumes "context" $ ) | nindent 8 }}
        {{- end }}
      {{- include "image.pullSecretsCognigy" $ | nindent 6 }}
      securityContext:
        {{- toYaml .securityContext | nindent 8 }}
{{- end }}