{{- if .Values.serviceFunctionScheduler.horizontalPodAutoscaler.enabled }}
{{- with .Values.serviceFunctionScheduler.horizontalPodAutoscaler }}
apiVersion: autoscaling/v2beta2
kind: HorizontalPodAutoscaler
metadata:
  name: service-function-scheduler
  namespace: {{ $.Release.Namespace | quote }}
spec:
  scaleTargetRef:
    kind: Deployment
    name: service-function-scheduler
    apiVersion: apps/v1
  minReplicas: {{ .minReplicas }}
  maxReplicas: {{ .maxReplicas }}
  metrics:
    {{- include "common.tplvalues.render" ( dict "value" .metrics "context" $ ) | nindent 4 }}
{{- end }}
{{- end }}
